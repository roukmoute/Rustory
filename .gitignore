# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri build output for files that live outside src-tauri/.
# Rules scoped to src-tauri/ are defined in src-tauri/.gitignore — do not
# duplicate them here.

# Local development artifact directory
.agents/

# Local SQLite runtime state (lives in app_data_dir, never in the repo)
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
