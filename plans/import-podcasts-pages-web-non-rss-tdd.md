# Plan TDD — import-podcasts-pages-web-non-rss

- État: `PRÊT`
- Contrat BDD: `708fc15c2c4aaf6edd56c82ae70e224d56619ad998d14c374768932b7825043c`
- Commit sélectionné: `b868a51704ae1db424f3a7ae5891db181fce1285`
- Feature: `features/import-podcasts-pages-web-non-rss.feature`
- Procédure QA: `stories/import-podcasts-pages-web-non-rss.md`

## Carte de couverture

- [x] Scénario: Importer une page web publique non-RSS → TDD-3, TDD-4, TDD-6, TDD-7
- [x] Exemple E1 de « Importer une page web publique non-RSS »: | https://www.radiofrance.fr/radiofrance/podcasts/selection-pour-partir-a-l-aventure | → TDD-6, TDD-7
- [x] Exemple E2 de « Importer une page web publique non-RSS »: | https://www.radiofrance.fr/franceinter/podcasts/serie-tina-et-le-serpent-a-plumes | → TDD-6, TDD-7
- [x] Scénario: Créer une histoire complète avec tous les épisodes de la page → TDD-6, TDD-7
- [x] Scénario: Importer un épisode sans image → TDD-4, TDD-6, TDD-7
- [x] Scénario: Refuser une URL mal formée → TDD-1, TDD-7
- [x] Scénario: Signaler une page inaccessible → TDD-2, TDD-7
- [x] Scénario: Signaler une page sans épisode audio → TDD-5, TDD-7
- [x] Scénario: Continuer d'importer un flux RSS → TDD-3

## Tranches TDD

### TDD-1 — Refuser une URL mal formée avant toute requête réseau
- Couvre: S4 « Refuser une URL mal formée » — adresse non http(s) valide → aucune requête réseau, raison de l'échec indiquée, aucune histoire créée.
- RED: Les deux premiers placeholders vides de `fetch_web_podcast_preview` (`src-tauri/src/commands/import_export.rs`, corps `assert!(true)`) deviennent des tests réels sans réseau : URL vide et schéma non http(s) (ex. `pas-une-url`) → la commande renvoie l'échec motivé existant `invalid_web_url_error()` ; une URL http(s) mal formée (ex. `http://`) échoue aussi à la validation, là où le check par préfixe actuel laisserait passer une sortie réseau.
- GREEN: Remplacer le check par préfixe `starts_with("http://")` de `preview_web_podcast` (et de l'accept à venir) par une validation stricte — schéma http/https + URL et hôte parsables, en réutilisant l'analyse déjà faite par `feed_url_host` — avec échec motivé renvoyé avant toute sortie réseau ; aucune histoire n'est créée.
- REFACTOR: Restreindre la validation à la garde d'entrée du parcours web, sans modifier le comportement RSS existant.
- Dépendances: aucune (fondation du parcours web).
- QA: Étape 7 de la procédure (S4) ; critères P5 / F4 : échec sans récupération en cours, raison affichée, aucune requête sortante, bibliothèque inchangée.
- Mutation d’acceptation: S4 de `features/import-podcasts-pages-web-non-rss.feature`.
- Mutation source: `src-tauri/src/application/import_export/web_episode_extraction.rs` et son point d'appel IPC.

### TDD-2 — Signaler une page inaccessible avec sa raison
- Couvre: S5 « Signaler une page inaccessible » — URL valide mais page injoignable ou erreur HTTP → raison de l'échec indiquée, aucune histoire créée.
- RED: Le test fragile `test_fetch_html_rejects_non_200_status` (httpstat.us) et le placeholder « propagates_network_error » de la commande deviennent des tests locaux déterministes : domaine réservé RFC 2606 (`.invalid`) → échec « injoignable » motivé ; petit serveur HTTP local répondant 500 → échec « erreur HTTP » motivé ; aucun des deux ne crée d'histoire.
- GREEN: `fetch_html` propage déjà l'échec de transport et le statut d'erreur HTTP vers un échec motivé ; garantir que les deux cas produisent une raison distincte (injoignable / statut HTTP) observable par l'utilisateur, sans créer d'histoire.
- REFACTOR: Uniformiser la représentation des échecs motivés du parcours web (une variante d'erreur par cas), sans changer les cas d'acceptation.
- Dépendances: TDD-1 (garde d'entrée d'abord).
- QA: Étape 8 de la procédure (S5) ; critères P6 / F5 : variante injoignable (domaine réservé `.invalid`) et erreur HTTP 500 (page locale).
- Mutation d’acceptation: S5 de `features/import-podcasts-pages-web-non-rss.feature`.
- Mutation source: le fetch de `src-tauri/src/application/import_export/web_episode_extraction.rs` et la suppression du test httpstat.us fragile.

### TDD-3 — Reconnaitre une source web non-RSS et préserver le chemin RSS
- Couvre: S1 (la source est reconnue comme une page web non-RSS) et S7 (un flux RSS valide est toujours reconnu RSS et importé par le comportement existant, sans changement).
- RED: Test échouant : une page HTML publique de fixture est prévisualisée comme source web non-RSS (hôte de la page dans l'aperçu) ; un flux RSS valide de fixture reste reconnu comme flux RSS et importé par le chemin existant inchangé (régression, sans réseau).
- GREEN: Le parcours web prévisualise la page comme source web non-RSS (hôte affiché dans l'aperçu) ; le chemin RSS (`rss_creation.rs`, commande `accept_rss_story_creation`) est laissé tel quel et couvert par la régression S7.
- REFACTOR: Factoriser la décision de routage de la source sans élargir le périmètre du parcours.
- Dépendances: TDD-2 (fetch motivé déjà en place).
- QA: Étape 2 (source reconnue dans l'aperçu E1/E2) et étape 10 (flux RSS public de référence) ; critères P1/P2 (reconnaissance), P8 / F7.
- Mutation d’acceptation: S1 (reconnaissance) et S7 de `features/import-podcasts-pages-web-non-rss.feature`.
- Mutation source: le point de routage de la source dans `src-tauri/src/application/import_export/` et `web_episode_extraction.rs`.

### TDD-4 — Extraire les épisodes de la page en ordre (titre, audio, image optionnelle)
- Couvre: S1 (au moins un épisode identifié, titre non vide, média audio, absence d'image tolérée) et S3 (épisode sans image importé sans erreur, champ image vide).
- RED: Tests échouant sur fixtures HTML locales : les épisodes d'une page de fixture (modèle des pages d'exemple, capturé à l'implémentation) sont extraits avec titre non vide + URL de média audio + image optionnelle, dans l'ordre d'apparition ; fixture d'un épisode sans image → `image_url` vide, sans erreur ; plus de titre de repli « Épisode sans titre ».
- GREEN: `parse_web_episodes` n'émet que des épisodes honnêtes : titre réel non vide, `audio_url` issu du média réellement présent dans le bloc d'épisode (éléments et liens audio des pages d'exemple), `image_url` seulement si la page la fournit, ordre du document conservé.
- REFACTOR: Supprimer les branches de selectors inutiles (repli `article`, `unwrap_or_else` sur selectors) sans élargir le périmètre.
- Dépendances: TDD-3 (reconnaissance de la source).
- QA: Étapes 2-3 (épisodes de l'aperçu E1/E2 : titres, médias audio, images) et étape 6 (page locale à un épisode sans image) ; critères P1/P2, P4 / F1, F3.
- Mutation d’acceptation: S1 (étapes d'identification des épisodes) et S3 de `features/import-podcasts-pages-web-non-rss.feature`.
- Mutation source: `parse_web_episodes` et `WebEpisode` de `src-tauri/src/application/import_export/web_episode_extraction.rs`.

### TDD-5 — Rejeter les épisodes invalides avec motif et signaler « aucun média audio »
- Couvre: S6 (page accessible sans aucun épisode avec média audio → signalement « aucun média audio n'a été trouvé », aucune histoire créée) et le rejet motivé des éléments invalides (constat d'audit).
- RED: Tests échouant : fixture avec un épisode à titre vide ou sans média audio → élément rejeté avec motif, absent de l'aperçu ; fixture sans aucun média audio → `preview_web_podcast` renvoie le signalement dédié ; le placeholder « propagates_parsing_error » de la commande devient ce test local (serveur HTTP local servant la page).
- GREEN: Filtrage dans `preview_web_podcast` : un épisode est retenu ssi titre non vide (après trim) + `audio_url` présent ; les éléments invalides sont rejetés avec motif (événements de diagnostic existants) ; zéro épisode valide → échec « aucun média audio n'a été trouvé », aucune histoire possible.
- REFACTOR: Isoler le filtrage des épisodes extraits dans une fonction testable, sans toucher au fetch ni au parsing.
- Dépendances: TDD-4 (extraction honnête).
- QA: Étape 9 de la procédure (page locale sans aucun média audio) ; critères P7 / F6 ; motif du rejet visible dans le rapport de l'aperçu.
- Mutation d’acceptation: S6 de `features/import-podcasts-pages-web-non-rss.feature`.
- Mutation source: `preview_web_podcast` de `web_episode_extraction.rs` et le signalement d'erreur dédié.

### TDD-6 — Exposer accept/commit en IPC et créer l'histoire complète prête à l'emploi
- Couvre: S2 (histoire complète créée : exactement N épisodes dans l'ordre d'apparition, titre propre à chaque épisode, audio téléchargé et associé, image associée si fournie) et le bout-en-bout S1/E1-E2.
- RED: Test échouant : sur une fixture de page à N épisodes valides, l'accept web (ré-fetch + re-parse) construit une structure canonique à N nœuds ordonnés — label du nœud = titre d'épisode, jamais le titre de la collection —, chaque audio est téléchargé et promu en ligne `assets` associée au nœud (chaîne `promote_enclosure` du RSS), l'image est associée si présente, et le commit (histoire + lignes `assets`) tient dans une seule transaction.
- GREEN: Nouvelle commande IPC `accept_web_podcast_creation` (miroir de `accept_rss_story_creation`) : préparation multi-épisodes, téléchargement des médias en phase réseau avant le verrou DB, création de l'histoire `source_format` web ; un échec de téléchargement d'un audio suit le comportement RSS existant (histoire créée, état partiel, motif honnête) ; l'accept mono-épisode WIP (`prepare_web_story_creation(selected_episode_title)`, `WebEpisodeRef`) est reformaté pour tous les épisodes.
- REFACTOR: Aligner les types du flux web (`PreparedWebCreation`) sur le schéma prepare/commit du RSS sans dupliquer le téléchargement.
- Dépendances: TDD-1 à TDD-5.
- QA: Étapes 1 à 6 de la procédure (E1, E2, compte N relevé sur la page elle-même, épisode sans image) ; critères P1 à P4 / F1, F2, F3.
- Mutation d’acceptation: S2, S1 (et S3) de `features/import-podcasts-pages-web-non-rss.feature`.
- Mutation source: `web_episode_extraction.rs` (prepare/commit multi-épisodes), `commands/import_export.rs` (commande + enregistrement dans `lib.rs`), `ipc/dto/import_export.rs`.

### TDD-7 — Parcours frontend : URL, aperçu, confirmation, échecs motivés
- Couvre: S1, S2, S3, S4, S5, S6 via l'interface visible : champ d'adresse URL, aperçu des N épisodes, confirmation de création, raison d'échec affichée, histoire dans la bibliothèque.
- RED: Test vitest échouant (modèle `CreateFromRssSurface.test.tsx`) : la surface web affiche la raison dans un `role="alert"` sur URL mal formée, page inaccessible et aucun média audio, liste les épisodes de l'aperçu (titre, audio, image) et émet l'accept à la confirmation ; le wrapper IPC web valide le contrat `WebPreviewDto` comme le wrapper RSS.
- GREEN: Ajouter `CreateFromWebSurface` + hook `use-web-creation` (modèle `use-rss-creation`) + wrappers IPC `fetch_web_podcast_preview` / `accept_web_podcast_creation` dans `src/ipc/commands/import-export.ts` + contrats partagés, et brancher la nouvelle entrée du parcours de création de `src/routes/library/LibraryRoute.tsx` : saisie URL → aperçu → « Créer l'histoire » → bibliothèque, sans sélection mono-épisode.
- REFACTOR: Réutiliser les composants partagés (`Button`, `Field`, `StateChip`, `aria-live`/alert) comme la surface RSS, sans dupliquer de styles.
- Dépendances: TDD-6 (accept exposé en IPC).
- QA: Toutes les étapes 1 à 10 de la procédure via l'interface ; critères P1 à P8 / F1 à F7.
- Mutation d’acceptation: S1 à S6 de `features/import-podcasts-pages-web-non-rss.feature`.
- Mutation source: `src/features/import-export/` (surface, hook, tests), `src/ipc/commands/import-export.ts`, `src/shared/ipc-contracts/import-export.ts`, `src/routes/library/LibraryRoute.tsx`.

## Alternatives rejetées

- 1. Conserver l'accept mono-épisode du WIP (`prepare_web_story_creation(selected_episode_title)` + `WebEpisodeRef`, structure minimale) — rejeté : le scénario S2 exige une histoire contenant exactement N épisodes dans l'ordre d'apparition sur la page ; une sélection mono-épisode ne peut le satisfaire. Remplacé par un accept multi-épisodes miroir de `accept_rss_story_creation`.
- 2. Un champ URL unique avec auto-routage RSS/web (sniffing dans une seule commande) — rejeté : le contrat ne définit pas le comportement d'une URL RSS saisie dans le parcours web ; le flux RSS existant reste l'entrée RSS (S7 sans changement) et le parcours web traite les pages HTML. Deux entrées distinctes, comme aujourd'hui, est le design minimal.
- 3. L'extraction via JSON-LD ou autres données structurées — rejeté : explicitement hors périmètre (aucun comportement de contrat) et non garanti sur les pages d'exemple ; les exigences observables (titre, média audio, image optionnelle, ordre) sont satisfaites par le HTML déjà présent dans les pages.
- 4. Conserver le test réseau externe httpstat.us — rejeté : non déterministe et instable ; remplacé par un serveur HTTP local (thread `std::net::TcpListener`) et un domaine réservé RFC 2606, à l'image des choix d'inputs de la story QA.
- 5. Suivre la pagination ou les liens « pages suivantes » — rejeté : explicitement hors périmètre (« une URL = une page »).

## Journal d'exécution

### Audit du point de départ (pages d'exemple, 2026-08-31)

1. Structure réelle des pages (HTML SSR SvelteKit, zéro `<audio>`, zéro URL `.mp3`/`.m4a` dans le HTML brut) :
   - Page liste E1 : bloc `<script type="application/ld+json">` avec `@graph` contenant un `ItemList` de 20 éléments ordonnés (`position` 1..20, `url` absolue de la page épisode) ; le blob SvelteKit embarqué ne porte que ~20 expressions (chargement progressif client, hors périmètre) ; aucune URL audio dans la page liste.
   - Page liste E2 : `ItemList` de 10 éléments.
   - Page épisode (ex. `l-ile-au-tresor-9062972`) : `@graph` avec nœud `RadioEpisode` : `name` (titre), `mainEntity` = `AudioObject` avec `contentUrl` (URL audio m4a sur `media.radiofrance-podcast.net`), `image.url` (ImageObject), `description`.
2. Vrai sol (fetch réel des 30 pages épisodes, HEAD des 27 audio) : E1 = 17/20 entrées résolues en épisode avec audio (les 3 entrées restantes pointent 3 fois la page série Tintin, sans `RadioEpisode`) ; E2 = 10/10. N observable : E1 = 17, E2 = 10.
3. Audio : format m4a (en-tête `ftyp`, marque `M4A `), tailles 12..106 Mo (max E1 = 106 118 738 o, E2 = 25 733 268 o) ; le plafond actuel `MAX_MEDIA_BYTES` = 32 MiB et `sniff_media` (png/jpeg/bmp/webp/mp3/wav/ogg) excluent le m4a → sans extension, tout audio réel dégénérerait en `(Media, Missing)` et violerait P3.
4. Code WIP : `fetch_html` (échecs transport/HTTP motivés déjà présents), `parse_web_episodes` (fallbacks `item`/`article` + titre de repli « Épisode sans titre », audio seulement via `enclosure[url]`/`a[href$='.mp3'…]`), `preview_web_podcast` (garde `starts_with("http://")`), `prepare_web_story_creation(selected_episode_title)` mono-épisode, `commit_web_story_creation` cassé (colonnes `stories` inexistantes), 4 placeholders `assert!(true)` dans `commands/import_export.rs` (~l. 1158-1180), commande `fetch_web_podcast_preview` enregistrée dans `lib.rs` (l. 550), aucun accept web exposé, test httpstat.us fragile.
5. Ancrages réutilisés : `feed_url_host` (domaine/import/rss.rs : http/https strict, sans userinfo, sans IPv6, port 1..=65535, hôte sobre) ; chaîne RSS `prepare_rss_story_creation`/`commit_rss_story_creation` (téléchargement en phase réseau, transaction `BEGIN IMMEDIATE` stories + assets + `story_local_imports`, verify-rows, compensation) ; `store_media` (sniff magic-bytes, store content-addressé, plafond dur 32 MiB) ; `CanonicalStructure` v3 (nœuds plats ordonnés `n1..`, `text`, `label`, `imageAssetId`, `audioAssetId`, `options[{label,target}]`).
6. Migrations : `story_local_imports.source_format` CHECK `IN ('rustory','structured-folder','rss')` (0013, table à reconstruire) ; `assets.media_format` CHECK `IN ('png','jpeg','mp3','wav','ogg')` (0007, à reconstruire avec l'index `idx_assets__content_hash` de 0008).

### Décisions d'implémentation (fixées à l'audit, sans ajout de contrat)

1. Extraction en trois passes dans l'ordre, un épisode n'est émis que s'il a un titre non vide ET une URL audio (rejet de tout fallback de titre) :
   - P1 — nœuds épisode JSON-LD directs (`RadioEpisode`/`PodcastEpisode`/`BroadcastEpisode`/`MusicRecording` avec `contentUrl`) ;
   - P2 — `ItemList` JSON-LD (tri `position`) : chaque URL résolue (dedup exact) est fetchée et extraite en P1/P3 (profondeur 1, pas de ré-ItemList) ; c'est le chemin des pages E1/E2 ;
   - P3 — DOM (scraper, ordre du document) : liens `<a href>` vers un média audio (extension `.mp3/.m4a/.ogg/.wav/.aac/.opus`), balises `<audio src>`/`<source src>` ; titre = texte de l'ancre (ou `aria-label`), image = `<img>` du conteneur (même `<li>`/parent proche).
2. Accept multi-épisodes (rejet du mono-épisode WIP) : re-fetch + re-parse, structure v3 à N nœuds ordonnés (`label` = titre d'épisode, `text` = description d'épisode sinon le titre, option `Continuer` vers le nœud suivant si le format de navigation l'exige), audio ET image (si fournie) téléchargés en phase réseau, commit en une seule transaction (`source_format = 'web'`).
3. m4a : migration 0016 (reconstruction `story_local_imports` + `'web'` et `assets` + `'m4a'`, index 0008 recréé), sniff `ftyp` + marques audio (`M4A ` observée, plus un petit jeu de marques audio), `mime_for_ext("m4a") = audio/mp4`, plafond média dédié au parcours web (128 MiB, paramétrable) sans toucher le plafond 32 MiB des autres flux (S7 inchangé octet à octet).
4. Zéro épisode audio extrait → échec motivé dédié « Aucun média audio n'a été trouvé » (S6) ; échecs de téléchargement individuels → verdict contenu `(Media, Missing)`, état `partial`, création poursuivie (précédent RSS).
5. Titre de l'histoire : titre de la collection/page si présent (JSON-LD `PodcastSeries.name`/`WebPage.name`, sinon `<title>`), normalisé+validé, repli `Histoire de {hôte}` (non observable par la QA, hypothèse 2 du plan).

#### TDD-1 — Refuser une URL mal formée avant toute requête réseau
1. RED — commande : `cd src-tauri && cargo test --lib test_fetch_web_podcast_preview_rejects` → exit 101 : `test_fetch_web_podcast_preview_rejects_invalid_scheme ... FAILED` — `assertion left == right failed, left: String("request"), right: "url_invalid"` (l'adresse `http://` traverse la garde par préfixe jusqu'au dispatch réseau) ; `..._rejects_empty_url ... ok`. Sortie RED : 1 passed, 1 failed, 1443 filtered out.
2. GREEN — garde d'entrée stricte : le helper `validate_web_entry_url` (`feed_url_host(web_url).ok_or_else(invalid_web_url_error)?`, renvoie l'hôte sobre) est consulté dans `preview_web_podcast` et `prepare_web_story_creation` AVANT tout fetch (remplace le check par préfixe `starts_with("http://")`). Commande : `cd src-tauri && cargo test --lib test_fetch_web_podcast_preview_rejects` → 2 passed, 0 failed, exit 0.
3. REFACTOR — helper commun placé après `ensure_web_source_enabled`, partagé par les deux façades web ; `source_host` dérivé de la garde (plus de `unwrap_or("unknown")`). Commande : `cd src-tauri && cargo test --lib web_episode_extraction` → 7 passed, exit 0.
4. Mutation source — commande : `cd src-tauri && cargo mutants --in-place -f 'src/application/import_export/web_episode_extraction.rs' -F 'validate_web_entry_url|invalid_web_url_error|preview_web_podcast|prepare_web_story_creation|ensure_web_source_enabled' -- --lib` → 7 mutants : 3 caught, 3 unviable, 1 missed, exit 2 (log `/tmp/tdd1_cargo_mutants2.out`). Survivant unique : `web_episode_extraction.rs:257:27 replace == with != in prepare_web_story_creation` — DISPOSITION DIFFÉRÉE : code WIP mono-épisode mort (non exposé en IPC, aucun test au TDD-1), réécrit intégralement au TDD-6 (la fonction disparaît ; à re-vérifier au run fichier du TDD-6), consigné et non masqué.
5. Mutation d'acceptation (APS) — scope S4. Chaîne (depuis le worktree root, chemins absolus hors worktree sous `/home/roukmoute/tmp-swarm-coder-aps/tdd1-s4/`) : `python3 src-tauri/tools/acceptance_scope.py features/…feature build-cible/feature.feature "Refuser une URL mal formée"` → `cd <APS-clone> && ~/.local/bin/bb gherkin-parser <feature.travail> ir.json` → `APS_FEATURE_PATH=<feature.travail> python3 src-tauri/tools/acceptance_entrypoint_generator.py ir.json generated/` → copie de `generated/entry_points.rs` dans le slot `src-tauri/tests/acceptance/generated/entry_points.rs` → `~/.local/bin/bb gherkin-mutator --level full --workers 1 --status-interval 0 --feature <feature.travail> --work-dir <tdd1-s4/mutation> --generated-dir <tdd1-s4/generated> --runner-worker "python3 <abs src-tauri/tools/acceptance_runner_worker.py>"` → `total=0 killed=0 survived=0 errors=0`, exit 0 (S4 sans table Examples → 0 mutations candidates ; scope exécuté et validé). `cd src-tauri && cargo test --test acceptance` → 1 passed (S4), exit 0. Feature gelée octet-à-octet identique au commit de base après le run (`cmp` contre `git show HEAD:features/…`).
6. Commit — `feat(web-import): TDD-1 — garde URL stricte avant tout réseau + infrastructure APS (scope, générateur, worker, harness S4)`.

#### TDD-2 — Signaler une page inaccessible avec sa raison
1. RED — commande : `cd src-tauri && cargo test --lib test_fetch_html` → exit 101 : les 3 tests nouveaux échouent — `test_fetch_html_rejects_unreachable_host_with_distinct_reason`, `test_fetch_html_rejects_http_error_status_with_distinct_reason`, `test_fetch_html_failure_reasons_are_distinct_per_case` — cause : `fetch_html` (code de base) renvoyait pour les 4 cas d'échec le MÊME message générique « Récupération de la page impossible. » (vérifié sur `git show HEAD:…web_episode_extraction.rs`) ; sortie RED : `assertion left != right failed, left: String("Récupération de la page impossible."), right: String("Récupération de la page impossible.")` (test de distinction) et échec du `contains("injoignable")` / `contains("erreur HTTP")` sur les 2 tests par cas.
2. GREEN — `fetch_html` renvoie désormais une raison DISTINCT et motivée par cas : `Request` → « La page est injoignable. » (stage `request`, code `RSS_SOURCE_UNREACHABLE`) ; `StatusCheck(status)` → « Le serveur a répondu avec une erreur HTTP {status}. » (stage `status_check`, détail `status`). Le placeholder `test_fetch_web_podcast_preview_propagates_network_error` de `commands/import_export.rs` devient un test réel sans réseau externe : adresse `.invalid` (RFC 2606) → `RSS_SOURCE_UNREACHABLE`, stage `request`, message non vide. Commandes : `cd src-tauri && cargo test --lib test_fetch_html` → 3 passed, exit 0 ; `cd src-tauri && cargo test --lib test_fetch_web_podcast_preview` → 4 passed, exit 0. Le test externe fragile httpstat.us (`test_fetch_html_rejects_non_200_status`) est supprimé et remplacé par le serveur HTTP local 500 déterministe (`LocalHttpServer`, 127.0.0.1:0, dans `mod tests` du module web).
3. REFACTOR — enum privée `WebFetchFailure` (une variante par cas : `ClientBuild`, `Request(String)`, `StatusCheck(u16)`, `ReadText(String)`) + méthode `into_app_error()` : un seul `match` centralise les 4 erreurs motivées (message, action, code, stage), `fetch_html` ne fait plus que mapper les erreurs transport. Régression complète : `cd src-tauri && cargo test --lib` → 1438 passed, 0 failed, 8 ignored, exit 0 (le chemin RSS S7 n'appelle pas `fetch_html`, privé au module web — inchangé).
4. Harness S5 — `src-tauri/tests/acceptance/handlers.rs` : 4 armements de dispatch sur (scénario, texte) pour « Signaler une page inaccessible » : Given documente en `SharedState` les 2 cas faisant foi du handler (domaine RFC 2606 `https://import-test-non-rss.exemple.invalid/` = injoignable ; serveur local 500 `LocalHttpServer` conservé vivant jusqu'à la fin du scénario) ; When `je lance l'import` → `preview_web_podcast(official_content_sources(), url, 30s)` par cas (un `Ok` = panic) ; Then `le système indique la raison de l'échec` → code stable `RSS_SOURCE_UNREACHABLE`, messages non vides, stage par cas (`request` / `status_check`), contenu par cas (« injoignable » / « erreur HTTP 500 »), et messages mutuellement DISTINCTS ; And `aucune histoire n'est créée` → helper `assert_no_story_created()` (DB in-memory, migrations, `SELECT count(*) FROM stories == 0`).
5. Mutation source — commande : `cd src-tauri && cargo mutants --in-place -f 'src/application/import_export/web_episode_extraction.rs' -F 'fetch_html|WebFetchFailure|into_app_error' -- --lib` → 4 mutants : 3 caught, 1 unviable, 0 missed, exit 0 (log `/tmp/tdd2_cargo_mutants.out`). Aucun survivant : les 2 cas S5 rendent chaque branche observable (`.invalid` → `request`, serveur local 500 → `status_check` + statut + message).
6. Mutation d'acceptation (APS) — scope CUMULATIF S4+S5 (règle du slot : le slot commité et `ir/base.json` couvrent tous les scénarios verts). Chaîne (chemins absolus hors worktree sous `/home/roukmoute/tmp-swarm-coder-aps/tdd2-s4s5/`) : `python3 src-tauri/tools/acceptance_scope.py features/…feature feature.feature "Refuser une URL mal formée" "Signaler une page inaccessible"` → `cd <APS-clone> && ~/.local/bin/bb gherkin-parser feature.feature ir.json` → `APS_FEATURE_PATH=feature.feature python3 src-tauri/tools/acceptance_entrypoint_generator.py ir.json generated/` (2 exécutions) → copie dans le slot `src-tauri/tests/acceptance/generated/entry_points.rs` + IR dans `src-tauri/tests/acceptance/ir/base.json` → `cd src-tauri && cargo test --test acceptance` → 2 passed (S4+S5), exit 0 → `~/.local/bin/bb gherkin-mutator --level full --workers 1 --status-interval 0 --feature feature.feature --work-dir mutation/ --generated-dir generated/ --runner-worker "python3 <abs acceptance_runner_worker.py>"` → `total=0 killed=0 survived=0 errors=0`, exit 0 (S4 et S5 sans table Examples → 0 mutations candidates ; scope exécuté et validé). Contrat gelé intact après le run : `cmp features/…feature <(git show HEAD:features/…feature)` → BYT-OK, idem story → BYT-OK.
7. Commit — `feat(web-import): TDD-2 — page inaccessible : raisons d'échec distinctes par cas (transport / HTTP) + harness S5, APS cumulatif S4+S5`.

#### TDD-3 — Reconnaitre une source web non-RSS et préserver le chemin RSS
1. RED — Unit : `cd src-tauri && cargo test --lib test_preview_web_podcast_extracts_item_list_episodes_in_position_order` → exit 101 : `the fixture list page must preview` (0 épisode sur une page de fixture au `ItemList` JSON-LD ; log `/tmp/tdd3_red_unit.out`). Acceptance : slot scope S1+S4+S5+S7 (`/home/roukmoute/tmp-swarm-coder-aps/tdd3-s1s4s5s7/`, 4 scénarios, 5 exécutions) → `cd src-tauri && cargo test --test acceptance` → exit 101 : `3 passed; 2 failed` — S1 E1+E2 échouent à handlers.rs l.464 `at least one épisode must be identified on the sample page` ; S4/S5/S7 verts (S7 bout-en-bout déjà opérationnel) ; log `/tmp/tdd3_red_acceptance.out`.
2. GREEN — `web_episode_extraction.rs` : passes JSON-LD P1/P2 (l'audit §1 : les pages d'exemple portent zéro `<audio>` et zéro `.mp3`/`.m4a` dans le HTML brut — les épisodes n'existent que dans le JSON-LD `ItemList` + nœuds `RadioEpisode` des pages épisode ; c'est la réalité des pages autorisée par l'objectif, pas un comportement de contrat) : `EPISODE_JSONLD_TYPES` (`RadioEpisode`/`PodcastEpisode`/`BroadcastEpisode`/`MusicRecording`), `collect_jsonld_nodes` (`@type` + récursion `@graph`), `jsonld_type` (string ou 1er élément d'array), `jsonld_payloads` (`script[type='application/ld+json']`, payloads inanalysables sautés), `audio_content_url` (`contentUrl` direct ou `mainEntity.contentUrl`, trim non vide), `episode_from_jsonld_node` (type reconnu + `name` trim non vide + audio ; `image_url: None` — l'image est portée par TDD-4), `resolve_url` (absolu tel quel / protocol-relative / root-relative / relatif nu contre le dossier de base), `episode_from_episode_page` (1er nœud reconnu en ordre document), `episodes_from_item_list` (tri `position` stable, absence → position document 1-based, dedup exact, fetch résolu profondeur 1, échec sauté honnêtement). Intégration dans `preview_web_podcast` : passe ItemList d'abord, repli DOM WIP (`parse_web_episodes`) si vide. S7 : le chemin RSS (`rss_creation.rs`, `accept_rss_story_creation`) est laissé tel quel. Commandes : `cd src-tauri && cargo test --lib` → 1441 passed, 0 failed, 8 ignored, exit 0 ; `cd src-tauri && cargo test --test acceptance` → 5 passed, 0 failed, exit 0 (S1 E1/E2 en conditions réelles, 11,5 s ; log `/tmp/tdd3_green_acceptance.out`).
3. REFACTOR — factorisation `extract_web_episodes(document, base_url, html, budget) -> Vec<WebEpisode>` (passe ItemList puis repli DOM), appelée par `preview_web_podcast` ; aucun changement de comportement : `cargo test --lib` → 1441 ok, `cargo test --test acceptance` → 5 ok.
4. Harness S1+S7 — `tests/acceptance/handlers.rs` (318 → 730 lignes) : `LocalHttpServer` généralisée en `FixtureHttpServer` (routes construites après bind ; le format de réponse S5 reste byte-identique, S5 migrée sur `("/page", 500, body)` + url `format!("{}/page", base)`). S1 : la Given documente consomme l'exemple URL (1× par ligne d'exemple), la When lance `preview_web_podcast` et stocke le `Result` tel quel, `s1_assert_source_recognized` (hôte de la source == hôte de l'URL, ≥1 épisode, titres non vides, audios non vides, image optionnelle — à TDD-3 toutes les `image_url` sont `None`, l'assertion sera ajustée en TDD-4). S7 : flux fixture local `("/flux")` + `("/media/episode-1.wav")` via `HttpRssFeedSource` → preview (guid `fixture-1`) → `accept_rss_story_creation` (DB in-memory + migrations, `store_root` temp) → preuve SQL : 1 story, titre `Episode un`, `structure_json` v3 dont le nœud de départ porte `audioAssetId`, `story_local_imports` (`rss`, hôte 127.0.0.1), 1 asset `wav`. Slot : `entry_points.rs` (5 tests) + `ir/base.json` (4 scénarios, S1 en `Scenario Outline` à 2 exemples).
5. Mutation source — commande (depuis `src-tauri/`) : `cargo mutants --in-place -f 'src/application/import_export/web_episode_extraction.rs' -F 'extract_web_episodes|episodes_from_item_list|episode_from_episode_page|episode_from_jsonld_node|audio_content_url|jsonld_payloads|collect_jsonld_nodes|jsonld_type|resolve_url|preview_web_podcast' -- --lib` → run 1 : 34 mutants, 24 caught, 5 unviable, 5 MISSED, exit 2 (log `/tmp/tdd3_cargo_mutants.out`). Les 5 survivants sont tous NON équivalents ; 4 tests de verrouillage comportemental ajoutés (aucun comportement ajouté, aucun code de production changé) : `test_resolve_url_pins_every_relative_branch` (les 5 branches relatives + url vide — tue `:256:33` `+ → -` et `+ → *`), `test_episode_page_with_array_type_is_recognized` (`@type` en array via ItemList + page épisode fixture — tue la suppression du bras `Value::Array` de `jsonld_type`, `:172:9`), `test_item_list_default_position_is_one_based_document_position` (position explicite 2 + position absente = position document 1-based 2, égalité → ordre document — tue `:312:41` `+ → *`), `test_item_list_keeps_an_exact_duplicate_url_once` (dedup des urls exactes en double — tue `:322:28` `&& → ||`). `cargo test --lib` → 1445 passed, 0 failed, 8 ignored, exit 0. Run 2 (même commande) : 34 mutants, 29 caught, 5 unviable, 0 missed, exit 0 (log `/tmp/tdd3_cargo_mutants_rerun.out`, preuve archivée `/tmp/mutants.out-tdd3/` dont `missed.txt` vide).
6. Mutation d'acceptation (APS) — scope CUMULATIF S1+S4+S5+S7 (slot `/home/roukmoute/tmp-swarm-coder-aps/tdd3-s1s4s5s7/`, 5 exécutions vertes avant mutation). Commande (depuis le clone APS) : `~/.local/bin/bb gherkin-mutator --level full --workers 1 --status-interval 0 --feature …/tdd3-s1s4s5s7/feature.feature --work-dir …/tdd3-s1s4s5s7/mutation --generated-dir …/tdd3-s1s4s5s7/generated --runner-worker "python3 <abs src-tauri/tools/acceptance_runner_worker.py>"` → `total=2 killed=1 survived=1 errors=0`, exit 1 (log `/tmp/tdd3_aps_mutation.out`). m1 = dither de l'URL E1 (`…/podcasts/…` → `…/pOdcasts/…`, changement de casse du PATH) → TUE (le serveur distingue la casse du path : la page n'est plus la page — `s1_preview` panique → `test_failure`). m2 = dither de l'URL E2 (`radiofrance.fr` → `radiofrance.fR`, changement de casse du DOMAINE) → SURVIVANT ÉQUIVALENT, preuve concrète : HTTP 200 sur `https://www.radiofrance.fr/franceinter/podcasts/serie-tina-et-le-serpent-a-plumes` ET sur `https://www.radiofrance.fR/…` (même page), le set des 10 URL d'épisodes extrait du JSON-LD `ItemList` est IDENTIQUE (sha256 du set trié `740be3eb60701ff6604793e8f63de329c046aaf19fb8aa337c168ca84ef86449` ; seul écart de corps : le script télémétrie CDN, variable par requête). RFC 3986 §3.2.2 : l'hôte d'une URL est insensible à la casse — la mutation ne change aucun comportement observable ; le passage du test est le comportement correct, aucun affaiblissement du handler. Contrat gelé intact après le run : `cmp features/…feature <(git show HEAD:features/…feature)` → BYTES-OK, idem story → BYTES-OK.
7. Commit — `feat(web-import): TDD-3 — reconnaissance source web non-RSS (JSON-LD ItemList/épisode) + régression RSS S7, APS cumulatif S1+S4+S5+S7`.

#### TDD-4 — Extraire les épisodes de la page en ordre (titre, audio, image optionnelle)
1. RED — Unit (3 tests en fin de `mod tests` de `web_episode_extraction.rs`) : `cd src-tauri && cargo test --lib` → exit 101, 2 failed, 8 ignorés (les totaux GREEN de référence : 1448 passed / 0 failed / 8 ignored). (a) `test_parse_web_episodes_extracts_dom_episodes_in_document_order` → `left: 0, right: 3` (zéro épisode sur la fixture DOM à 3 médias titrés + 1 ancre vide qui ne doit pas être émise). (b) `test_episode_page_image_is_carried_when_provided_and_absent_otherwise` → `left: None, right: Some("…/images/avec-image.jpg")` (l'image JSON-LD n'était pas portée sur l'épisode). (c) `test_parse_web_episodes_never_invents_a_fallback_title` → VERT dès le RED : le repli `article` du WIP est du code mort (le `else` de `if let Ok(Selector::parse("item"))` ne se déclenche jamais) — le test est conservé comme pin de régression.
2. GREEN — `web_episode_extraction.rs` : réécriture de `parse_web_episodes(html_content, base_url) -> Vec<WebEpisode>` — groupe de selectors `a[href], audio[src], audio source[src]` (ordre document garanti par la traversée) ; par média, `dom_episode_media(&media) -> Option<(String, &str)>` : « a » → href doit finir par une extension audio (`DOM_AUDIO_EXTENSIONS: [&str; 6] = .mp3/.m4a/.ogg/.wav/.aac/.opus`, comparaison en minuscules), titre = texte de l'ancre trim, sinon aria-label trim, sinon None ; « audio » → src + aria-label ; « source » → src + aria-label de l'élément `<audio>` parent (`parent_element()` via le trait `Element`) ; titre ou url vides → None (rien d'inventé, plus de « Épisode sans titre ») ; `resolve_url(base_url, raw)` → None = skip ; déduplication par url audio résolue (`seen_audio_urls`) ; `media_container` (parent le plus proche, à l'exception de `<source>` → parent du `<audio>`), `container_image_url` (premier `img[src]` non vide, résolu), `container_summary` (premier `<p>` trim, sinon ""). `WebEpisode { title, summary, audio_url: Some(…), image_url }`. Le titre DOM n'utilise JAMAIS un heading (S2 : le titre de la collection ne remplace jamais le titre de l'épisode). Image JSON-LD : `jsonld_image_url(node)` (champ `image`) → `image_url_from_jsonld_value` (String → `trimmed_url` ; Object → `.get("url")` ; Array → 1er élément récursif ; sinon None) → portée telle quelle, SANS résolution (comme l'audio `contentUrl`) ; `episode_from_jsonld_node` porte désormais `image_url`. Call sites : `extract_web_episodes` → `parse_web_episodes(html, base_url)` ; `prepare_web_story_creation` (WIP, supprimé au TDD-6) → `parse_web_episodes(&html_content, web_url)`. Commandes : `cd src-tauri && cargo test --lib` → 1448 passed, 0 failed, 8 ignored, exit 0 (log `/tmp/tdd4_lib_green.out`) ; `cd src-tauri && cargo test --test acceptance` → 6 passed, 0 failed, exit 0 (S1 E1/E2 en conditions réelles — sur les vraies pages les images deviennent `Some`, le handler ajusté les accepte ; log `/tmp/tdd4_aps_scope_green.out`).
3. Harness S3 + ajustement S1 — `tests/acceptance/handlers.rs` : `s1_assert_image_optional` passe à la nouvelle sémantique (image optionnelle : si `Some` → url non vide ; aucune assertion de présence). 4 handlers S3 : état `s3_server/s3_page_url/s3_preview` + accessors, dispatch `("Importer un épisode sans image", <étapes exactes>)`, impl : fixture locale `/page` avec un seul `<a>` titré vers un `.m4a` et AUCUN `<img>` → `preview_web_podcast` → assert Ok + items non vides + `image_url` None sur tous les items. Slots régénérés au scope TDD-4 : `entry_points.rs` (6 tests) + `ir/base.json` (5 scénarios, 6 exécutions).
4. REFACTOR — le repli `item`/`article` mort et les `unwrap_or_else` de selectors inutiles (périmètre du plan) sont supprimés dans la réécriture elle-même ; re-runs `cargo test --lib` et `cargo test --test acceptance` verts (mêmes totaux que GREEN).
5. Mutation source — commande (depuis `src-tauri/`) : `cargo mutants --in-place -f 'src/application/import_export/web_episode_extraction.rs' -F 'parse_web_episodes|WebEpisode|dom_episode_media|media_container|container_image_url|container_summary|jsonld_image_url|image_url_from_jsonld_value|trimmed_url|episode_from_jsonld_node' -- --lib` → run 1 : 44 mutants, 33 caught, 6 unviable, 5 MISSED, exit 2 (log `/tmp/tdd4_cargo_mutants.out`, archive `/tmp/mutants.out-tdd4-run1/`). Les 5 survivants, tous NON équivalents : `:221:9` suppression du bras `Value::String(url)` de `image_url_from_jsonld_value` ; `:226:9` suppression du bras `Value::Array(images)` ; `:450:9` suppression du bras `"source"` de `dom_episode_media` ; `:511:5` `container_summary` → `String::new()` ; `:511:5` `container_summary` → `"xyzzy".into()`. (Les 6 unviable = `Default::default()` sur des types sans `Default` ; cet outil ne génère pas de mutation de statement dans le corps de la boucle de `parse_web_episodes` — même limitation observée au TDD-3.) 5 tests de verrouillage ajoutés, sans aucun changement de code de production (extension des 2 tests existants) : le fixture DOM gagne un `<p>Résumé deux.</p>` dans sa section 2 + assertion `episodes[1].summary == "Résumé deux."` (tue les 2 mutants `container_summary`) ; le fixture DOM gagne une section 4 `<audio aria-label="Episode source"><source src="…/episode-source.opus"></source></audio>` + assertions titre/url/image None (tue le bras `"source"`) ; le fixture image JSON-LD gagne 2 épisodes (position 2 `/episodes/image-string` avec `"image"` string, position 3 `/episodes/image-array` avec `"image"` array) + 2 pages fixture + 2 routes + assertions `items[1]`/`items[2]` (tuent les bras `String`/`Array`) ; len 3 → 4 (DOM) et 2 → 4 (image) avec les assertions correspondantes. `cd src-tauri && cargo test --lib` → 1448 passed, 0 failed, 8 ignored, exit 0 (log `/tmp/tdd4_lib_lock.out`) ; `cd src-tauri && cargo test --test acceptance` → 6 passed, 0 failed, exit 0 (log `/tmp/tdd4_accept_lock.out`). Run 2 (même commande) : 44 mutants, 38 caught, 6 unviable, 0 missed, exit 0 (log `/tmp/tdd4_cargo_mutants_rerun.out`, archive `/tmp/mutants.out-tdd4/` dont `missed.txt` vide).
6. Mutation d'acceptation (APS) — scope CUMULATIF S1+S3+S4+S5+S7 (scratch `/home/roukmoute/tmp-swarm-coder-aps/tdd4-s1s3s4s5s7/`, 6 exécutions vertes avant mutation). Chaîne : `python3 src-tauri/tools/acceptance_scope.py features/…feature <scratch>/feature.feature "Importer une page web publique non-RSS" "Importer un épisode sans image" "Refuser une URL mal formée" "Signaler une page inaccessible" "Continuer d'importer un flux RSS"` → `bb gherkin-parser` + `acceptance_entrypoint_generator.py` (6 exécutions) → slots copiés dans le worktree → `cargo test --test acceptance` 6/6 → depuis le clone APS : `~/.local/bin/bb gherkin-mutator --level full --workers 1 --status-interval 0 --feature …/tdd4-s1s3s4s5s7/feature.feature --work-dir …/mutation --generated-dir …/generated --runner-worker "python3 …/acceptance_runner_worker.py"` → `total=2 killed=1 survived=1 errors=0`, exit 1 (log `/tmp/tdd4_aps_mutation.out` : m1 `test_failure`, m2 `test_success`). m1 = dither de l'URL E1 (`podcasts` → `pOdcasts`, casse du PATH) → TUE (la page n'est plus la page). m2 = dither de l'URL E2 (`radiofrance.fr` → `radiofrance.fR`, casse du DOMAINE) → SURVIVANT ÉQUIVALENT, preuve TDD-3 : HTTP 200 sur les deux URLS, set des 10 URL d'épisodes IDENTIQUE (sha256 du set trié `740be3eb60701ff6604793e8f63de329c046aaf19fb8aa337c168ca84ef86449`), RFC 3986 §3.2.2 (l'hôte d'une URL est insensible à la casse) — le passage du test est le comportement correct, aucun affaiblissement du handler. Contrat gelé intact après le run : `cmp features/…feature <(git show HEAD:features/…feature)` + story → BYTES-OK.
7. Commit — `feat(web-import): TDD-4 — extraction DOM honnête (titre/audio/image optionnelle) + image JSON-LD, APS cumulatif S1+S3+S4+S5+S7`.

#### TDD-5 — Rejeter les épisodes invalides avec motif et signaler « aucun média audio »
1. RED — commande : `cd src-tauri && cargo test --lib` → exit 101 : les 2 tests nouveaux échouent — `test_preview_web_podcast_reports_no_audio_media_when_page_has_none` (fixture locale SANS aucun média audio : le preview renvoie `Ok(WebPreviewOutcome { items: [] })` au lieu du refus motivé) et `test_keep_valid_episodes_rejects_untitled_and_audioless_items` (4 items → seul « Valide » attendu) — 1447 passed / 2 failed / 8 ignored (log `/tmp/tdd5_red_lib.out`).
2. GREEN — `web_episode_extraction.rs` : filtre `keep_valid_episodes` (retenu ssi titre non vide après trim ET `audio_url` `Some` non vide après trim) appelé dans `preview_web_podcast` après `extract_web_episodes` ; résultat vide → `no_audio_media_error()` dédié (`import_failed("Aucun média audio n'a été trouvé.", "Vérifie que la page contient des épisodes audio puis réessaie.")` + details `{source: "parsing", stage: "no_audio_media"}`). `commands/import_export.rs` : le placeholder WIP `test_fetch_web_podcast_preview_propagates_parsing_error` (`assert!(true)`) devient le test réel `test_fetch_web_podcast_preview_reports_page_without_audio_media` (serveur local minimal `S6FixtureServer`, une page sans audio) ; le bras `Err` de `fetch_web_podcast_preview` est honnête : `code = serde_json::to_value(&err.code)` (wire SCREAMING_SNAKE de `AppErrorCode`) + `source = error_detail(&err, "source")`. Le test WIP `test_preview_web_podcast_carries_page_host_as_source` est adapté : sa fixture gagne un épisode DOM honnête (`<a href="…/media/episode.wav">Episode</a>`), car une page sans audio est désormais un refus motivé S6. Commande : `cd src-tauri && cargo test --lib` → 1450 passed, 0 failed, 8 ignored, exit 0 (log `/tmp/tdd5_lib_green.out`).
3. REFACTOR — jugé déjà minimal (filtre `keep_valid_episodes` isolé dans une fonction testable, `no_audio_media_error` dédié, wiring 3 lignes dans `preview_web_podcast`, noms clairs) → aucun changement ; re-run `cargo test --lib` → 1450/0/8, exit 0 (log `/tmp/tdd5_lib_refactor.out`).
4. Harness S6 + slots 7 exécutions — `tests/acceptance/handlers.rs` : `s6_server/s6_page_url/s6_refusal` dans `SharedState` + accessors, 4 entrées de dispatch sur S6, `s6_document_audioless_page` (fixture locale 200 sans aucun média audio), `s6_run_import` (`preview_web_podcast` réel + `IMPORT_BUDGET` ; `Ok` = panic), `s6_assert_report` (message contractuel exact + `details.stage == no_audio_media`), And = `assert_no_story_created()`. Coquille corrigée : le helper acceptance exige `Vec<u8>` pour le body (la fixture servait `String`, E0308, 1er run exit 101) → `.as_bytes().to_vec()` (le helper lib homonyme accepte `String` : deux signatures distinctes, normal). Slots régénérés au scope cumulatif S1+S3+S4+S5+S6+S7 (6 scénarios / 7 exécutions) : `generated/entry_points.rs` (7 fns) + `ir/base.json`. Commande : `cd src-tauri && cargo test --test acceptance` → 7 passed, 0 failed, exit 0 (16,28 s, S1 = réseau réel) (log `/tmp/tdd5_acceptance.out`).
5. Mutation d'acceptation (APS) — scope cumulatif S1+S3+S4+S5+S6+S7 (scratch `/home/roukmoute/tmp-swarm-coder-aps/tdd5-s1s3s4s5s6s7/`). Chaîne : `acceptance_scope.py` → `bb gherkin-parser` (chemins absolus, cwd = clone APS) → `acceptance_entrypoint_generator.py` (7 exécutions) → copie des slots → acceptance 7/7 → depuis le clone APS : `bb gherkin-mutator --level full --workers 1 --status-interval 0 … --runner-worker "python3 …/acceptance_runner_worker.py"` → `total=2 killed=1 survived=1 errors=0`, exit 1 (log `/tmp/tdd5_aps_mutation.out`). m1 = dither du PATH d'E1 (`podcasts` → `pOdcasts`) → TUE (la page n'est plus la page). m2 = dither du DOMAINE d'E2 (`radiofrance.fr` → `radiofrance.fR`) → SURVIVANT ÉQUIVALENT, preuve TDD-3 réutilisée : RFC 3986 §3.2.2 (l'hôte est insensible à la casse), HTTP 200 sur les deux URL, set d'URL d'épisodes identique (sha256 du set trié `740be3eb…449`). Contrat gelé intact après le run : `cmp` feature + story contre HEAD → BYTES-OK.
6. Mutation source — commande (depuis `src-tauri/`) : `cargo mutants --in-place -f 'src/application/import_export/web_episode_extraction.rs' -F 'preview_web_podcast|keep_valid_episodes|no_audio_media_error' -- --lib` → 7 mutants : 4 caught (`:527:5` filtre → `vec![]`, `:531:17` `&&` → `||`, `:530:13` et `:534:32` suppression de `!` dans `keep_valid_episodes`), 3 unviable (`Default::default()` sur des types sans `Default` : `Vec<WebEpisode>`, `AppError`, `Result<WebPreviewOutcome, AppError>`), 0 missed, exit 0 (log `/tmp/tdd5_cargo_mutants.out`, archive `/tmp/mutants.out-tdd5-run1/`, `missed.txt` vide). Le missed différé du TDD-1 (`:257:27` dans `prepare_web_story_creation` WIP) est hors de ce filtre : code mort toujours prévu pour suppression au TDD-6.
7. Commit — `feat(web-import): TDD-5 — rejet des épisodes invalides + signalement « aucun média audio », APS cumulatif S1+S3+S4+S5+S6+S7`.

#### TDD-6 — Accept/commit multi-épisodes exposé en IPC (S2, E1/E2)
1. RED — commande `cd src-tauri && cargo test --lib` → exit 101, échec de compilation causé par les tests nouveaux qui référencent l'API manquante, capté sur 3 fronts : migrations (log `/tmp/tdd6_red_migrations.out` : `migration_0016_allows_web_provenance_and_refuses_unknown_formats` + `migration_0017_allows_m4a_assets_and_refuses_unknown_formats` FAILED — 1 passed / 2 failed / 1458 filtered out) ; node_media (log `/tmp/tdd6_red_node_media.out` : `store_media_capped` « not found in this scope », E0425 ×5, compilation échouée) ; web (log `/tmp/tdd6_red_web.out` : E0425 `WEB_MAX_MEDIA_BYTES` manquant à `node_media.rs:659` + E0061 `prepare_web_story_creation` attend 4 args, 5 fournis à `web_episode_extraction.rs:1734`).
2. GREEN — `web_episode_extraction.rs` : accept multi-épisodes miroir de `accept_rss_story_creation` — `prepare_web_story_creation` (5e arg `staging: Option<&Path>` ; le WIP mono-épisode `selected_episode_title` est supprimé) ; `commit_web_story_creation_tx` : re-fetch + re-parse de la page à zéro (la source est l'autorité — le wire ne porte qu'un pointeur), refus typé `sourceChanged` si le checksum diverge, téléchargement + promotion de chaque média via la chaîne RSS existante (`promote_enclosure`), lignes `assets` dans la même transaction, relecture de preuve `verify_rows` DANS la transaction avant composition du DTO de succès (un succès n'est jamais composé sur des données non prouvées). `node_media.rs` : plafond web élargi `WEB_MAX_MEDIA_BYTES` (128 Mo), bras `m4a` du sniff (ISOBMFF `ftyp` + marque `M4A `) et de `mime_for_ext` (`audio/mp4`), `store_media_capped(cap)` + `store_media` déléguant (plafond local 32 Mo inchangé), borne de relecture élargie ; 2 tests de bordure (fichier exactement au plafond lu en entier / plafond+1 refusé) + refus strict de `store_media_capped`. `commands/import_export.rs` : commande IPC `accept_web_podcast_creation` exposée, événements de diagnostic web (`web_failure_event` porte le code d'erreur ; `web_failure_log_host` ne lit jamais l'adresse sur un refus de politique) ; `dto/import_export.rs` : `WebCreationOutcomeDto` (`created{story,report} | sourceChanged`) ; migrations `0016_web_provenance.sql` (`source_format 'web'` + refus des formats inconnus) + `0017_media_m4a.sql` ; tests de migration dans `db/mod.rs` ; `import_log.rs` : parité des diagnostics web (écart au plan, justifié par la parité RSS de l'audit) ; `Cargo.toml` : dev-dep `image` (tests media). Commande : `cd src-tauri && cargo test --lib` → 1471 passed, 0 failed, 8 ignored (1479 tests), exit 0 (log `/tmp/tdd6_lib_full.out`, marqueur `EXIT_LIB=0` ligne 1488).
3. REFACTOR — jugé déjà minimal (chaîne accept/commit isomorphe du chemin RSS, noms porteurs `prepare_web_story_creation` / `commit_web_story_creation_tx` / `verify_rows`, garde de relecture documentée) → aucun changement ; les 1471 tests restent verts (log ci-dessus).
4. Harness S2 + slots cumulatifs — `tests/acceptance/handlers.rs` : slots S2 (fetch + accept bout-en-bout sur les pages d'exemple : N épisodes dans l'ordre d'apparition, titre propre à chaque épisode jamais remplacé par le titre de collection, audio téléchargé + promu + associé, image associée si fournie, refus motivés S4/S5/S6, régression RSS S7) + 2 entrées de dispatch ; `generated/entry_points.rs` + `ir/base.json` régénérés au scope cumulatif S1..S7 (7 scénarios / 8 exécutions — S1 = les 2 exemples faisant foi). Commande : `cd src-tauri && cargo test --test acceptance` → 8 passed, 0 failed, exit 0 (18,47 s, S1 = réseau réel E1+E2) (log `/tmp/tdd6_acceptance_rerun.out`).
5. Mutation d'acceptation (APS) — la feature n'a pas changé depuis le run précédent (empreinte `708fc15c2c4aaf6edd56c82ae70e224d56619ad998d14c374768932b7825043c` revérifiée par `swarm-bdd-contract fingerprint`) → le set de mutants APS est identique, re-exécuté (log `/tmp/tdd6_aps_mutation.out`) → `total=2 killed=1 survived=1 errors=0`. m1 = dither du PATH d'E1 (`podcasts` → `pOdcasts`) → TUE (la page n'est plus la page). m2 = dither du DOMAINE d'E2 (`radiofrance.fr` → `radiofrance.fR`) → SURVIVANT ÉQUIVALENT : RFC 3986 §3.2.2 (l'hôte est insensible à la casse), HTTP 200 sur les deux URL, set d'URL d'épisodes identique (sha256 du set trié `740be3eb…449`). Aucun mutant nouveau n'est possible sans changement de la feature gelée.
6. Mutation source — depuis `src-tauri/` (cargo-mutants 27.1.0, `--minimum-test-timeout 90`) :
   - `cargo mutants --in-place -f 'src/application/import_export/web_episode_extraction.rs' -- --lib` → 4 runs itératifs (`/tmp/tdd6_mutants_web.out` → `web2.out` → `web3.out` → `web4.out`, convergence après ajout des tests ciblés : 7 missed → 2 → 1 → 1) ; run final : 22 mutants : 17 caught, 4 unviable, 1 missed, exit 2 (log `/tmp/tdd6_mutants_web4.out`). Le seul missed = `:996:28` `||`→`&&` dans la garde de relecture `verify_rows` de `commit_web_story_creation_tx` : ÉQUIVALENT DOCUMENTÉ — la relecture s'exécute dans la même transaction que l'écriture, sans écrivain concurrent : un écart partiel (titre conforme / état divergent) n'est pas atteignable, aucun trigger vérifiable.
   - `cargo mutants --in-place -f 'src/infrastructure/filesystem/node_media.rs' -- --lib` → run de référence : 65 mutants : 46 caught, 2 unviable, 17 missed (log `/tmp/tdd6_mutants_nodemedia.out`). Scoping : 4 missed dans `read_file_bounded` (code modifié par la slice : borne `MAX_MEDIA_BYTES` → `WEB_MAX_MEDIA_BYTES`) → re-run scoped `-F 'read_file_bounded'` sur l'arbre final : 8 mutants : 8 caught, 0 missed, exit 0 (log `/tmp/tdd6_mutants_nodemedia_rfb.out`) — les 2 tests de bordure 128 Mo tuent l'intégralité des mutants de la fonction, dont les 4 missed du run de référence ; les 13 missed restants (9 mutants de la logique MP3 de `sniff_media` `:225-229` + 4 suppressions de bras `mime_for_ext` jpg/jpeg/wav/ogg/mp3 `:246-249`) sont pré-existants, hors scope de la slice → transférés au hardener.
   - Garde de régression sur la chaîne RSS partagée (la fonction RSS unique appelée par le chemin web est `promote_enclosure`, `rss_creation.rs:153`) : `cargo mutants --in-place -f 'src/application/import_export/rss_creation.rs' -F 'promote_enclosure' --minimum-test-timeout 90 -- --lib` → 2 mutants : 1 caught, 1 unviable, 0 missed, exit 0 (log `/tmp/tdd6_mutants_rfb_rerun.out`) ; le run plus large de la session précédente (8 mutants : 8 caught, exit 0, log `/tmp/tdd6_mutants_rfb.out`) couvrait la même chaîne — son filtre exact n'est pas recoverable, la re-run ci-dessus couvre la dépendance RSS de la slice.
   - `cargo mutants --in-place -f 'src/commands/import_export.rs' -- --lib` → run 1 : 7 mutants : 5 missed, 2 unviable, exit 2 (log `/tmp/tdd6_mutants_importexport.out`) → 2 tests ajoutés au module `tests` (`web_failure_event_dispatches_on_the_error_code` + `web_failure_log_host_never_reads_the_address_on_a_policy_refusal`) → run 2 : 5 caught, 2 unviable, 0 missed, exit 0 (log `/tmp/tdd6_mutants_importexport2.out`).
7. Commit — `feat(web-import): TDD-6 — accept/commit web multi-épisodes (IPC + m4a + provenance web), APS cumulatif S1..S7`.

#### TDD-7 — Parcours frontend de création depuis une page web non-RSS (S1..S6 via l'interface)
1. RED — commande `pnpm exec vitest run src/shared/ipc-contracts/import-export.test.ts src/ipc/commands/import-export.test.ts src/features/import-export/components/CreateFromWebSurface.test.tsx` → exit 1 : 3 fichiers failed, 10 failed / 165 passed (175) (log `/tmp/tdd7_red_vitest.out` — les tests référencent des exports absents : le kind `web` + ses garde-fous de contrat, les wrappers `fetchWebPodcastPreview`/`acceptWebPodcastCreation`, le composant `CreateFromWebSurface`) ; `pnpm exec vitest run src/features/import-export/hooks/use-web-creation.test.ts` → « no tests » : le module `use-web-creation` n'existe pas (log `/tmp/tdd7_red_hook.out`).
2. GREEN — `shared/ipc-contracts/import-export.ts` : kind `web` ajouté à `ContentSourceKind` + label figé « Page web », `isContentSourceEntry` (ligne web enabled = pas de raison + marqueur exact ; fail-closed pour tout autre kind activé), DTOs `WebPreview` (`sourceHost`, `pageChecksum` 64 hex, `items[{title, summary, audioUrl, imageUrl}]`) et `WebCreationOutcome` (`created{story,report} | sourceChanged`) avec gardes strictes miroir à l'octet de `dto/import_export.rs`. `ipc/commands/import-export.ts` : wrappers typés `fetchWebPodcastPreview(webUrl)` / `acceptWebPodcastCreation(webUrl, pageChecksum)` rejetant tout payload dérivé via `WebCreationContractDriftError` (le composant n'appelle jamais `invoke` directement). `hooks/use-web-creation.ts` : machine à états deux phases (idle → fetching → review{sourceChanged?} → creating → created / failed / unavailable), jeton de génération (un résultat qui atterrit après un abandon est ignoré — jamais de surface ressuscitée), refus de politique `CONTENT_SOURCE_UNAVAILABLE` → état calme dédié `unavailable` (jamais rendu comme une casse), invalidation du cache bibliothèque uniquement après un accept réussi. `components/CreateFromWebSurface.tsx` (+ `.css`) : idle (ligne de posture + champ « Adresse de la page web » + CTA « Récupérer la page »), progrès fetching/creating, review (l'hôte en tête — jamais l'adresse complète, les N épisodes de l'aperçu dans l'ordre du document avec marqueurs « Média audio »/« Image », CTA « Créer l'histoire » refusé — `aria-disabled` — tant que l'adresse tapée diverge de celle reviewée), alertes motivées S4/S5/S6 (champ conservé + « Réessayer »), verdict figé si la page a changé, chip de succès. `CreateStoryDialog.tsx` : entrée « Démarrer depuis une page web » (fail-closed si le handler n'est pas câblé, raison Rust si la politique refuse, exclusivité inter-flux, close-puis-handover). `LibraryRoute.tsx` : `useWebCreation` + surface + câblage dialog→surface→accept→invalidate, `isWebCreationActive` dans TOUS les ensembles d'exclusivité (OS-open, drop, CTA d'import, entrées folder/archive/RSS du dialog). Commandes : `pnpm exec vitest run` par suite ciblée → contrats 121/121, IPC 54/54, hook 9/9, surface 12/12 (dont le test `addressDiverged` calque exact du test RSS homonyme), dialog 37/37, route 154/154 ; suite complète `pnpm test` → 113 fichiers, 1923 passed, 0 failed, exit 0 (log `/tmp/tdd7_pnpm_test_final.out`) ; `pnpm run typecheck` → exit 0.
3. REFACTOR — jugé minimal (miroir structurel du parcours RSS : hook/surface/dialog de même forme, garde de divergence, jeton de génération et copies figées mono-source ; aucune duplication de logique) → aucun changement ; les 1923 tests restent verts (log ci-dessus).
4. Mutation source — campagne de 14 mutants sur le code frontend de la slice (application → run vitest ciblé → restauration, intégrité vérifiée par hash après chaque restauration) : 13 TUE, 1 équivalent. M1 contrat : bras `web` de `isContentSourceEntry` → rejeter enabled → TUE (2 failed / 121, `import-export.test.ts`) ; M2 IPC : arg wire `{webUrl}` → `{url}` → TUE (`ipc/commands/import-export.test.ts`) ; M3 IPC : garde de contrat de `acceptWebPodcastCreation` supprimée → TUE (idem) ; M4 hook : check de génération retiré dans `fetchPreview` (late settlement après abandon) → TUE (`use-web-creation.test.ts`) ; M5 hook : comparaison `sourceChanged` inversée → TUE (3 failed / 9, idem) ; M6 hook : refus de politique → `failed` au lieu de `unavailable` → TUE (idem) ; M7 surface : `showFetchCta` → toujours vrai → TUE (`CreateFromWebSurface.test.tsx`) ; M8 surface : `addressDiverged` → toujours faux → TUE (idem — le test ajouté en 2) ; M9 dialog : garde `isCreateFromWebUnavailable` retirée de `handleCreateFromWeb` → ÉQUIVALENT DOCUMENTÉ : le primitif `Button` (`src/shared/ui/Button.tsx`) avale lui-même le clic quand `aria-disabled` — le handler n'est joignable que par ce bouton, la garde est une defense-in-depth redondante, aucune différence observable ; M10 route : `isWebCreationActive` retiré de `isOsOpenBlockedByLiveFlow` → TUE (`LibraryRoute.test.tsx`) ; M11 idem de `isDropBlockedByLiveFlow` → TUE (idem) ; M12 idem de l'arg busy de `renderCenter` → TUE (idem) ; M13 route : `onCreateFromWebRequest` → mauvais handler (`setIsRssCreationOpen`) → TUE (5 failed / 154, idem) ; M14 route : `onAccept` de la surface → mauvais flux (`rssCreation.acceptCreation`) → TUE (idem).
5. Mutation d'acceptation (APS) — feature inchangée (empreinte `708fc15c2c4aaf6edd56c82ae70e224d56619ad998d14c374768932b7825043c` revérifiée par `swarm-bdd-contract fingerprint`) → le set de mutants APS est identique au run TDD-6 : `total=2 killed=1 survived=1` (m1 = dither PATH d'E1 → TUE ; m2 = dither DOMAINE d'E2 `radiofrance.fr` → `radiofrance.fR` → ÉQUIVALENT RFC 3986 §3.2.2, log `/tmp/tdd6_aps_mutation.out`) ; aucun mutant nouveau possible sans changement de la feature gelée.
6. Acceptance + global — `cd src-tauri && cargo test --test acceptance` → 8 passed, 0 failed, exit 0 (18,47 s, S1 = réseau réel E1+E2) (log `/tmp/tdd6_acceptance_rerun.out`) ; `pnpm test` → 1923/1923 exit 0 et `pnpm run typecheck` → exit 0 (logs ci-dessus).
7. Commit — `feat(web-import): TDD-7 — parcours frontend création depuis page web non-RSS`.
